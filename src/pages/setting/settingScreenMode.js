import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const SettingScreenMode = () => {
  return (
    <View>
    </View>
  );
}

const styles = StyleSheet.create({})

export default SettingScreenMode;
